<?xml version="1.0" encoding="utf-8"?>
<!--
  AlertaYa — política de red (solo debug).

  Permite HTTP plano únicamente a IPs de red local (Wi-Fi, emulador,
  dispositivo en LAN) para poder apuntar la app al backend en desarrollo.
  Este archivo NO se empaqueta en builds de release (source set debug-only).

  Si tu máquina tiene otra IP local, agregala como <domain> abajo.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- Emulador Android (10.0.2.2 = host) y localhost -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>

        <!-- IPs de la red local (agregar las que uses) -->
        <domain includeSubdomains="false">192.168.18.21</domain>
        <domain includeSubdomains="false">192.168.0.151</domain>
    </domain-config>
</network-security-config>
