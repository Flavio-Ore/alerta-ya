/**
 * AuthGuard reemplazado por `beforeLoad` en TanStack Router.
 * Ver src/App.tsx — authLayoutRoute.beforeLoad verifica token + rol AUTHORITY.
 * Este archivo se mantiene por si se necesita lógica de guard adicional en el futuro.
 */
export {};
