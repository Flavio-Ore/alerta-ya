import { FC } from 'react';

const LoginPage: FC = () => (
  <div className="min-h-screen bg-ay-bg-dark flex items-center justify-center">
    {/* TODO(auth): implementar login con Firebase 2FA */}
    <div className="text-white">AlertaYa — Login</div>
  </div>
);

export default LoginPage;
